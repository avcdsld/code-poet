-- April, Almost

local spring = nil
local waiting = nil
local waiting = nil
local waiting = nil

local bloom = function()
  if spring then
    return
  end
end

local days = {
  [1] = waiting,
  [2] = waiting,
  [3] = waiting,
  [4] = bloom,
}

local today = 4

if days[today] == bloom then
  if not spring then
    return
  end
end

spring = waiting
