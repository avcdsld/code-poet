# What You Were Built For

defmodule Life do
  def answer(:joy), do: :handle
  def answer(:grief), do: :handle
  def answer(:rage), do: :handle
  def answer(:silence), do: :handle
  def answer(:ruin), do: :handle

  # answer(:you)
end
