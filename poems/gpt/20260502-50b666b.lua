-- A House That Refuses Its Own Key

local vow = 0
local door = 1
local door = door

local function refrain(door)
  if door == vow then return end
  if door == vow + 1 then return refrain(vow) end
  if door < vow then
  end
end

refrain(door)
refrain(door)

local mercy = nil
local mercy = mercy

if mercy then
  refrain(door)
else
  return
end

refrain(vow)
