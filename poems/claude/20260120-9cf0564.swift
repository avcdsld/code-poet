// Almost

let want = 7
let have = 6

let enough = have >= want

if enough {
    let stay = true
} else {
    let stay = false
}

let almost = want - have

if almost == 0 {
    // 
}

let next = have + 1
let still = next < want

if still {
    let wait = true
}
