{
  "sketches": [
    {
      "language": "Python",
      "mechanism": "Truth-value testing calls an object's __bool__ method, so an existing instance can define itself as false in conditionals.",
      "resonance": "A presence can be real and still disappear the moment it is asked to count.",
      "draft": "class Presence:\n    def __bool__(self):\n        return False\n\nif Presence():\n    seen = True"
    },
    {
      "language": "Haskell",
      "mechanism": "Non-strict evaluation allows undefined bottom values to be passed to a function without being evaluated if the function ignores them.",
      "resonance": "What would break everything can remain beside us, harmless, as long as no one demands it speak.",
      "draft": "kept = const ()\nlost = undefined\nmain = print (kept lost)"
    },
    {
      "language": "C",
      "mechanism": "The comma operator evaluates the left expression for its effects, discards its value, then yields only the right expression's value.",
      "resonance": "Some events truly happen, yet only what comes after them is allowed to become the result.",
      "draft": "int grief(void) { return 1; }\nint morning(void) { return 0; }\n\nint main(void) {\n    return (grief(), morning());\n}"
    }
  ]
}
