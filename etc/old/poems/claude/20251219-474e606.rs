// Almost Enough

fn main() {
    let almost = 99;
    let enough = 100;

    if almost >= enough {
        return;
    }

    let waiting = almost + 1;
    let waiting = waiting - 1;
    let waiting = waiting;

    let _ = enough;
    let _ = waiting;

    if false {
        let arrival = ();
        let _ = arrival;
    }
}
