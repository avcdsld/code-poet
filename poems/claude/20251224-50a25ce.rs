// Almost Midnight

fn main() {
    let hours = 23;
    let minutes = 59;
    let seconds = 59;

    let almost = hours == 23 && minutes == 59;
    let never = hours == 24;

    if never {
        return;
    }

    if almost {
        let waiting = seconds;
        let waiting = waiting;
        let waiting = waiting;
    }

    let tomorrow = never;
}
