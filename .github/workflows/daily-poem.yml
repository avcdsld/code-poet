name: daily-poem

on:
  schedule:
    - cron: "5 15 * * *" # JST 00:05
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-gpt:
    runs-on: ubuntu-latest
    environment: auto-poet
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: node engine/generate.mjs gpt
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
      - name: Commit and push changes
        run: |
          git add poems/gpt/ prompts/gpt/ manifests/gpt/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Generate daily poem (GPT) for $(date +%Y-%m-%d)"
            git push
          fi

  generate-claude:
    runs-on: ubuntu-latest
    environment: auto-poet
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: node engine/generate.mjs claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      - name: Configure Git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
      - name: Commit and push changes
        run: |
          git add poems/claude/ prompts/claude/ manifests/claude/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Generate daily poem (Claude) for $(date +%Y-%m-%d)"
            git push
          fi
